# squad-room web

Static frontend for Squad Room.

Open `index.html` directly or publish this folder through GitHub Pages.

The default API endpoint is:

```text
http://localhost:8787
```

The frontend does not store or send provider API keys.

